# Enforce UTF-8 + LF for all text; UTF-16 source silently breaks GitHub Actions
# YAML parsing, .gitignore matching, and every grep/diff tool.
* text=auto eol=lf

*.d      text eol=lf
*.sdl    text eol=lf
*.yml    text eol=lf
*.adoc   text eol=lf
*.md     text eol=lf
